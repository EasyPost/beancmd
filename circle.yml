dependencies:
  pre:
    - sudo apt-get install beanstalkd
  override:
    - pip install tox tox-pyenv
    - pyenv install 2.6.8
    - pyenv local 2.6.8 2.7.12 3.5.3 3.6.2
test:
  override:
    - tox

# vim: set syntax=yaml ts=2 sw=2 sts=0 expandtab:
